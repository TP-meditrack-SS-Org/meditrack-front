name: CI Angular

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  ci-angular:
    name: Angular CI (Node ${{ matrix.node-version }})
    strategy:
      fail-fast: false
      matrix:
        node-version: ["18", "20"]
    uses: TP-meditrack-SS-Org/shared-workflow-ci/.github/workflows/ci-express.yml@main
    with:
      node-version: ${{ matrix.node-version }}
      test-command: npm run test:ci

